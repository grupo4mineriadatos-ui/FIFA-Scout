# Stack COMPLETO fijado con versiones exactas, verificadas como mutuamente
# compatibles entre si y con wheels precompilados para Python 3.11 (fijado
# en "Advanced settings" al crear el deploy en Streamlit Community Cloud).
# No usar rangos (">=") aca: en los intentos anteriores, cualquier paquete
# sin version exacta termino siendo el que fallaba al importar en runtime
# porque el resolver (uv/pip) eligio una version incompatible o el build
# se corto antes de instalarlo.
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.5.2
joblib==1.4.2
pandas==2.2.2
streamlit==1.38.0
plotly==5.24.1
matplotlib==3.9.2
shap==0.46.0
numba==0.60.0
llvmlite==0.43.0
