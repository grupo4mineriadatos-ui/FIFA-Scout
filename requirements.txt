# Nucleo ML: fijado EXACTO porque debe coincidir con el entorno donde se
# genero modelo_gb_pipeline.joblib (evita el ModuleNotFoundError al
# deserializar). Si conoces las versiones reales de tu entorno de
# entrenamiento, reemplaza estos 4 numeros por esos.
scikit-learn==1.5.2
numpy==1.26.4
joblib==1.4.2
scipy==1.13.1

# SHAP y su cadena de dependencias (numba/llvmlite) son la causa mas comun
# de builds colgados: si no se fijan exactas, pip prueba muchas
# combinaciones (backtracking) o termina compilando llvmlite desde
# codigo fuente, que puede tardar mas de una hora o directamente fallar
# en el hardware gratuito de Streamlit Cloud. Estas 3 versiones son
# mutuamente compatibles y tienen wheels p
